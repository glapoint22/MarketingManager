import { NonFeaturedSearchPipe } from './non-featured-search.pipe';

describe('NonFeaturedSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new NonFeaturedSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
